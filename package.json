{
  "name": "modpack-builder",
  "displayName": "Modpack Builder",
  "description": "'npm run build' to create a release with the version set in pack.toml in releases/",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "scripts": {
    "build": "powershell .\\.vscode\\BuildRelease.ps1"
  }
}
